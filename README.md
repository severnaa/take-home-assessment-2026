### Installation

1. pull down the repo.
2. `npm install --no-save`
3. `npm run db:create-db`
4. `npm run start`
5. Navigate to http://localhost:3000/

### Usefull scripts

- `npm run test`
- `npm run start`
